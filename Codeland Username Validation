import re


def CodelandUsernameValidation(strParam):
  if len(strParam) >= 4 and len(strParam) <= 25:
    if re.match("^[a-zA-Z][a-zA-Z0-9_]*[a-zA-Z0-9]$", strParam):
      return strParam
      return true

# keep this function call here 
print(CodelandUsernameValidation(input()))
